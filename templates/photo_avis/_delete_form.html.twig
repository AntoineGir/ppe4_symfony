<form method="post" action="{{ path('photo_avis_delete', {'id': photo_avi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photo_avi.id) }}">
    <button class="btn">Delete</button>
</form>
