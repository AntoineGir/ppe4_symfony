{% extends 'base.html.twig' %}

{% block title %}PhotoAvis{% endblock %}

{% block body %}
    <h1>PhotoAvis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo_avi.id }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ photo_avi.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('photo_avis_index') }}">back to list</a>

    <a href="{{ path('photo_avis_edit', {'id': photo_avi.id}) }}">edit</a>

    {{ include('photo_avis/_delete_form.html.twig') }}
{% endblock %}
