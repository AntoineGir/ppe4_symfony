{% extends 'base.html.twig' %}

{% block title %}PhotoAvis index{% endblock %}

{% block body %}
    <h1>PhotoAvis index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for photo_avi in photo_avis %}
            <tr>
                <td>{{ photo_avi.id }}</td>
                <td>{{ photo_avi.photo }}</td>
                <td>
                    <a href="{{ path('photo_avis_show', {'id': photo_avi.id}) }}">show</a>
                    <a href="{{ path('photo_avis_edit', {'id': photo_avi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('photo_avis_new') }}">Create new</a>
{% endblock %}
